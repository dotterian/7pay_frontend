/* @flow */

import React, { Component } from 'react';

type Props = {
  className?: string
};

class Header extends Component<Props> {
  render() {
    return <h1>Header</h1>;
  }
}

export default Header;
